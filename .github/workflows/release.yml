name: Build and Release

on:
  push:
    tags:
      - 'v*' # Trigger on version tags like v1.0.0

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: FntGenerator-Linux
            asset_name: FntGenerator-Linux.tar.gz
          - os: windows-latest
            arch: x64
            artifact_name: FntGenerator-Windows-x64
            asset_name: FntGenerator-Windows-x64.zip
          - os: windows-latest
            arch: Win32
            artifact_name: FntGenerator-Windows-x86
            asset_name: FntGenerator-Windows-x86.zip
          - os: macos-latest
            artifact_name: FntGenerator-macOS
            asset_name: FntGenerator-macOS.zip

    steps:
      - uses: actions/checkout@v3

      # --- Linux Dependencies ---
      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libglfw3-dev libfreetype6-dev libgtk-3-dev xorg-dev zenity

      # --- macOS Dependencies ---
      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install glfw freetype

      # --- Build (Common) ---
      - name: Configure CMake
        shell: bash
        run: |
          if [ "${{ runner.os }}" == "Windows" ]; then
            cmake -B build -A ${{ matrix.arch }} -DCMAKE_BUILD_TYPE=Release
          elif [ "${{ runner.os }}" == "macOS" ]; then
            cmake -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64"
          else
            cmake -B build -DCMAKE_BUILD_TYPE=Release
          fi

      - name: Build
        run: cmake --build build --config Release

      # --- Packaging ---
      
      # Windows: Zip the Executable
      - name: Package Windows
        if: runner.os == 'Windows'
        run: |
          cp -r res build/Release/
          cd build/Release
          powershell Compress-Archive -Path FntGenerator.exe, res -DestinationPath ../../${{ matrix.asset_name }}

      # macOS: Zip the .app Bundle
      - name: Package macOS
        if: runner.os == 'macOS'
        run: |
          cd build
          zip -r ../${{ matrix.asset_name }} FntGenerator.app ../res

      # Linux: Tar the Executable
      - name: Package Linux
        if: runner.os == 'Linux'
        run: |
          cp -r res build/
          cd build
          chmod +x FntGenerator
          tar -czvf ../${{ matrix.asset_name }} FntGenerator res

      # --- Upload Release Assets ---
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ matrix.asset_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
